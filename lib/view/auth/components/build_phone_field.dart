import 'package:flutter/cupertino.dart';

class BuildPhoneMenuItems extends StatelessWidget {
  const BuildPhoneMenuItems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
